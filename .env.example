# Local development only: browser-side Gemini key.
VITE_GEMINI_API_KEY=

# AI provider selector (future providers can be added without UI changes).
VITE_AI_PROVIDER=gemini

# Keep false by default. In production, use a server-side AI endpoint.
VITE_ALLOW_BROWSER_AI_KEY=false
